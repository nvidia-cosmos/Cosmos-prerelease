# Python bytecode caches — never check in.
__pycache__/
*.pyc
*.pyo
*.pyd
*$py.class

# Editor / OS noise.
*.swp
.DS_Store

# Local training output (generated, large).
training_output/
outputs/

# Release-tool metadata (regenerated on every release run).
cosmos_training_meta/
